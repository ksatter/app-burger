<h1> Eat-Da-Burger</h1>

<h4> Available </h4>


<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{>burger-block devoured=false}}
    {{/unless}}

  {{/each}}
</ul>

<hr>

<h4> Burgers that were </h4>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{>burger-block devoured=true}}
    {{/if}}
    {{else}}
    <h4>You haven't eaten any burgers! Time to get chomping!</h4>
  {{/each}}
</ul>

<hr>

<h4> Order A New Burger </h4>

<div class="row">
  <form class="col s10">
    <div class="input-field">
      <input placeholder="What would you like on your burger?" id="new_burger" type="text" class="validate" required=""/>
      <label for="new_burger">What'll it be?</label>
    </div>

    <button type="submit">Place Order</button>
  </form>
</div>
